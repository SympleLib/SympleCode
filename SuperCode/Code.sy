using char* as String;

decl int Print wprintf(String str, String, int);

struct Human
{
	String name,
	int coolness,
}

int Run()
{
	Human best;
	best.name = 'Treidex';
	best.coolness = 99999;
	Print('Name: \'%s\', Coolness (100%% Accurate): %i\n', best.name, best.coolness);
	ret 0;
}