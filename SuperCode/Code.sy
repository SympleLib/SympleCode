struct Type
{
	char* name,
	uint size,
}

decl void printf(char* fmt, char*, uint);

long Run()
{
	Type ty;
	ty.name = 'int';
	ty.size = 32;
	printf('Name: \'%s\', Size: %u\n', ty.name, ty.size);

	ret 0;
}