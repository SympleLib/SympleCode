decl void MessageBoxA(byte*, schar* content, schar* title, uint type);
decl void MessageBoxW(byte*, wchar* content, wchar* title, uint type);

long Run()
{
	MessageBoxA(0, s'Test', s'Hello!', 0);
	MessageBoxW(0, w'Test', w'Hello!', 0);

	ret 0;
}