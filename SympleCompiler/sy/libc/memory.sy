extern ptr malloc(int size);
extern ptr calloc(int size);
extern ptr realloc(int size);
extern void free(ptr item);

extern ptr memset(ptr obj, byte val, int count);
extern ptr memcpy(ptr dest, ptr source, int size);