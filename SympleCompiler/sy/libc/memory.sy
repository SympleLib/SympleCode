extern ptr memchr(ptr buff, byte block, int size) ccall;
extern int memcmp(ptr left, ptr right, int size) ccall;
extern ptr memcpy(ptr dest, ptr source, int size) ccall;
extern ptr memmove(ptr dest, ptr source, int size) ccall;
extern ptr memset(ptr buff, byte val, int repeat) ccall;

extern ptr malloc(int size) ccall;
extern ptr calloc(int count, int elementSize) ccall;
extern ptr realloc(ptr old, int size) ccall;
extern void free(ptr buff) ccall;