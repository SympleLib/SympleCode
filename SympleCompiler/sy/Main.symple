#include "libc/io.sy"

struct Person
{
	char* Name;
	int Age;
};

hint void PrintPerson(Person);

int main()
{
	Person me = Person { "Teddy", 12 };

	PrintPerson(me);
	
	0;
};

void PrintPerson(Person p)
{
	printf("Name: '%s', Age: %i\n", p.Name, p.Age);
};