#include "libc/io.sy"
#include "libc/memory.sy"

struct Pos
{
	mut int x, y;
};

int main()
{
    mut Pos pos;
	pos.y = 21;
	pos.x = 14;
	
	printf("(Int) %i + %i = %i\n", pos.x, pos.y, pos.x + pos.y);
	printf("(Bool) %i + %i = %i\n", !!pos.x, !!pos.y, !!(!!pos.x + !!pos.y));
	
	0;
};