extern int printf(string fmt);
extern int puts(string str);
extern int putc(char c, ptr stream);
extern ptr __acrt_iob_func(int num);

#define stdin  __acrt_iob_func(0)
#define stdout __acrt_iob_func(1)
#define stderr __acrt_iob_func(2)

int main()
{
	int myInt = 6 * 9;
	byte myByte = 4 * 20;
	
	int sum = -myInt + myByte;
	
	printf("-myInt + myByte = %i\n",sum );
	
	myInt = myByte / 3;
	
	printf("myByte / 3 = %i\n", myInt);

	printf("Number: %i, Even: %s\n", myInt, BoolToString(!(myInt % 2)));
	char newLine = '\n';
	putc(newLine, stdout);

	printf("Are they the same? %s\nCoincedence I think Not!!!\n", BoolToString(myInt == sum));
	
	while true;
		
	0;
};

string BoolToString(bool boolean)
	if (boolean)
		"true";
	else
		"false";