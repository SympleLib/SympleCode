decl void printf(char* fmt, ...)
decl void puts(char*)

void run {
	Test x

	printf('x = %i, y = %i, z = %i, w = %i\n', x.x, x.y, x.z, x.w)
	puts('Hello, world!')
}

struct Test {
	int x = 69,
	int y = 420,
	int z = 1337,
	int w,
}