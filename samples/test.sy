// link 'link.sy';

@naked decl int MessageBoxW(byte*, char*, char*, int);

#define MB_ICONQUESTION 0x20

class MessageBox {
	char* title, message,
	int type;
	
	int show() {
		ret MessageBoxW(null, message, title, type);
	}
}

void run() {
	MessageBox msg = MessageBox { 'Question', 'does it work? 🤔', @MB_ICONQUESTION };
	msg.show();
}