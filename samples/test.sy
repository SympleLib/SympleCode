public decl void printf(char* fmt, ...) @cdecl
public decl void putchar(char) @cdecl
public decl void puts(char*) @cdecl

public decl void MessageBoxA(byte* wnd, char* text, char* title, int type) @stdcall

public void run {
	int* run = int [ false, 6.9, 44.2, test() ];
	printf('%i, %i, %i, %i\n', run[0], run[1], run[2], run[3]);
	
	msg();
}

int test -> 45;

void msg ->
	MessageBoxA(0, 'Test Works!', 'Test', 0);