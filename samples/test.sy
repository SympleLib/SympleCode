@naked decl int MessageBoxW(byte*, char*, char*, int);

int MessageBox_IconQuestion = 0x20;

class MessageBox {
	char* title, message,
	int type;
	
	int show() {
		ret MessageBoxW(null, message, title, type);
	}
}

void run() {
	MessageBox msg = MessageBox { 'Question', 'does it work? 🤔', MessageBox_IconQuestion };
	msg.show();
}