decl void printf(char* fmt, ...)
decl void puts(char*)

int sdf;

void run {
	Test x;
	printf('x = %i, y = %i, z = %i, w = %i\n', x.x, x.y, x.z, x.w);
	puts(x.a.test);
	puts('Hello, world!');
}

struct Test {
	int x = 69,
	int y = 420,
	int z = 1337,
	int w = sdf + 1,
	X a,
}

struct X {
	char* test = 'haha this works, I am so proud!',
}