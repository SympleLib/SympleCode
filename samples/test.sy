@naked decl void wprintf(char* fmt, ...);

class Test {
	char** msg1, msg2, msg3;
	
	void printMe() {
		wprintf('"%s" "%s" "%s"\n', msg1, msg2, msg3);
	}
}

@entry void main() {
	Test test = Test { 'helo', 'guys', 'hehe' };
	testFn(test);
}

void testFn(Test test) {
	test.printMe();
}