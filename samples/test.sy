@naked decl int MessageBoxW(byte*, char*, char*, int);
@naked decl byte* malloc(int size);
@naked decl void printf(char* fmt, ...);

int MessageBox_IconQuestion = 0x20;

class MessageBox {
	char* title, message,
	int type;
	
	mut int show() {
		message = 'get haxed nerd';
		ret MessageBoxW(null, message, title, type);
	}
	
	int show() {
		ret MessageBoxW(null, message, title, type);
	}
}

@entry void asjdf083wjhjr5930() {
	mut int* ptr = stalloc(3 * 4);
	ptr[0] = 69;
	ptr[1] = 420;
	ptr[2] = 	1337;
	printf("%i\n", %ptr);
}